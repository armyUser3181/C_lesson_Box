# C_lesson_Box
crazy

## 다음에 계속

YED를 활용한 define 객체를 생성합니다

YED 리턴값은 다음 시퀀스의 여부를 결정합니다.

stack up -> up; switch( main() ) 

function (): for {
    switch (main()) {
        case up: function;
        case dewn break;
    }
    return;
}