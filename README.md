# C_lesson_Box
crazy

## 다음에 계속

YED를 활용한 define 객체를 생성합니다

YED 리턴값은 다음 시퀀스의 여부를 결정합니다.

stack up -> up; switch( main() ) 

function (): for {
    switch (main()) {
        case up: function;
        case dewn break;
    }
    return;
}

## next

### conteainer

 1. 컨테이너는 메모리를 저장하는 struct입니다. 메모리를 저장하는 표준 규격으로 제정이 되었습니다.

#### Auque

 1. 블록 형태로 데이터를 저장합니다.
 2. 데이터를 저장할 때, 데이터의 크기를 기준으로 블록을 생성합니다.
 3. 특수한 함수를 사용하여 데이터 가공이 가능합니다.

## ground struct

 1. 특정한 형태의 함수를 불러오는 아키텍처입니다.
 2. 포커스를 가진 struct를 객체처럼 사용이 가능합니다.

  